# Brave Fencer Musashi (USA) — SLUS-00726
# Values read directly from the PS-X EXE header of SLUS_007.26
# (extracted from the disc root, LBA 24) and SYSTEM.CNF (LBA 23).

[game]
name         = "Brave Fencer Musashi"
id           = "SLUS-00726"
exe          = "games/musashi/SLUS_007.26"
load_address = "0x80010000"
entry_pc     = "0x80010000"
text_size    = "0x00064800"
stack_base   = "0x801FFFF0"
disc         = "games/musashi/Brave Fencer Musashi (USA).cue"

[recompiler]
# No Ghidra seeds yet — discovery walks the call graph from entry_pc alone.
seeds   = "games/musashi/seeds.txt"
out_dir = "games/musashi/generated"
strict  = true

[video]
# OpenGL is the backend this game is actually developed and tested against
# (the software rasterizer is slow enough here that the compositor treats the
# window as unresponsive during boot). Baked in so no CLI flag is needed.
renderer          = "opengl"

# 16:9 via native-wide (the default wide strategy): the wider field of view is
# rendered into an actually wider frame and presented 1:1, with no GTE squash
# and no per-primitive correction — so it needs no [widescreen] block.
#
# This was previously unset, because the only 3D-frame detector was the
# per-game sprite-tag path: with no tags ws_game_mode() was always false, every
# frame pillarboxed, and the banner claimed "widescreen 16:9" while the picture
# stayed 4:3 (ISSUES.md Issue #9). The detector is now general — gte.cpp stamps
# the frame on RTPS/RTPT, which is direct evidence the frame drew 3D — so the
# setting does what it says here without identifying any of the game's own
# drawing functions.
#
# Note this window is LARGER than 4:3 on a short screen: 4:3 at 1280 is
# 1280x960 and gets clamped down to fit, 16:9 at 1280 is 1280x720 and does not.
aspect_ratio      = "16:9"

# 2x ordered-grid supersampling. Fill-rate cost is ~N^2, which is nothing for
# PS1-era geometry on any modern GPU; 3 or 4 also work if the edges still read
# too jagged.
supersampling     = 2

[runtime]
debug_port   = 4370
window_title = "Brave Fencer Musashi (PSXRecomp)"
controller   = "digital"
memcard_dir  = "."

# 4x CD-ROM timing. Deliberately NOT "instant": the README documents that as
# hanging some games during early init, with 4x as the recommended value until
# that is root-caused. FMV playback reverts to 1x automatically whenever XA
# audio is streaming, so movies keep their intended pacing either way.
disc_speed   = "4x"

# Capture overlays to overlay_captures.json so they can be compiled to native
# code offline and dispatched natively on later runs. Off by default in the
# framework, which meant BFM captured nothing all through the first play
# session — and BFM keeps real game logic in overlays (the damage store lives
# at 0x8014BCB4, well past the 0x74800 text end), so with this off the parts
# that matter are never accelerated and never available for offline analysis.
overlay_cache = true


# LLE (the real recompiled BIOS intro) is the only boot path that reaches the
# title screen today. Under HLE the guest wedges in an event poll loop and no
# content frame is ever drawn. Investigated and written up as ISSUES.md
# Issue #8 — including two hypotheses eliminated by measurement, so a future
# pass does not repeat them. Flip back to true once that issue closes.
bios_hle = false
